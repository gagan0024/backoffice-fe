server {
    listen 4173;

    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }

    error_page 404 /index.html;

    location /assets/ {
        root /usr/share/nginx/html;
    }
}
